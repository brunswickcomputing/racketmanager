# RacketManager plugin .gitignore (CI/CD-friendly)

# OS/filesystem noise
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies and build artifacts (should not be committed)
node_modules/
vendor/
dist/
build/
coverage/
.cache/
.tmp/

# Local test scratch areas (keep if used locally only)
/tests
/my-tests
*phpunit.*

# Local binaries/output
/bin/*

# Keep config and CI files tracked (do NOT ignore .github or dotfiles globally)
# Ensure this file itself is tracked
!.gitignore
