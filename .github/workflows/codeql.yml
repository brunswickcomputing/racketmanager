name: CodeQL (deprecated)

# This workflow is deprecated and kept only for manual runs as a noop.
# Use .github/workflows/codeql-analysis.yml instead.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. Use codeql-analysis.yml."
