{
  "name": "racketmanager-plugin",
  "private": true,
  "version": "10.0.0",
  "description": "Build tools for the RacketManager WordPress plugin",
  "scripts": {
    "build": "esbuild src/js/index.js --bundle --outfile=dist/js/racketmanager.js --format=esm --sourcemap --target=es2020 && esbuild css/style.css css/admin.css css/modal.css css/print.css --outdir=dist/css --sourcemap",
    "build:prod": "esbuild src/js/index.js --bundle --outfile=dist/js/racketmanager.js --format=esm --minify --target=es2020 && esbuild css/style.css css/admin.css css/modal.css css/print.css --outdir=dist/css --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
